import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Notificações", "Suas notificações e alertas.");

export default function NotificationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
