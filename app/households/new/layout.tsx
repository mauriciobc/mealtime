import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Nova residência", "Crie um novo lar.");

export default function HouseholdNewLayout({ children }: { children: React.ReactNode }) {
  return children;
}
