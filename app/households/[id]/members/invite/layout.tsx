import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Convidar membros", "Convide pessoas para seu lar.");

export default function InviteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
