import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Editar residência", "Edite as informações do lar.");

export default function HouseholdEditLayout({ children }: { children: React.ReactNode }) {
  return children;
}
