import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Residências", "Gerencie seus lares e domicílios.");

export default function HouseholdsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
