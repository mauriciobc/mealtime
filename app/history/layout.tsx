import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Histórico", "Histórico de alimentação e eventos.");

export default function HistoryLayout({ children }: { children: React.ReactNode }) {
  return children;
}
