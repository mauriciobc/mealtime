openapi: 3.0.0
info:
  title: MealTime API
  version: 2.0.0
  description: |
    API completa para gerenciamento de alimentação de gatos
    
    **VERSÕES DISPONÍVEIS**:
    - **V2 (Recomendado)**: `/api/v2/*` - Autenticação JWT/Session, respostas padronizadas
    - **V1 (Deprecated)**: `/api/*` - Será removido em 2025-07-28
    
    **AUTENTICAÇÃO V2**:
    - Mobile: JWT via header `Authorization: Bearer <token>`
    - Web: Supabase Session via cookies
  contact:
    name: MealTime Support
    email: support@mealtime.app

servers:
  - url: http://localhost:3000/api/v2
    description: Servidor V2 (desenvolvimento) - RECOMENDADO
  - url: https://mealtime.app/api/v2
    description: Servidor V2 (produção) - RECOMENDADO
  - url: http://localhost:3000/api
    description: Servidor V1 (desenvolvimento) - DEPRECATED
  - url: https://mealtime.app/api
    description: Servidor V1 (produção) - DEPRECATED

tags:
  - name: Auth
    description: Autenticação e autorização
  - name: V2 - Cats
    description: 🆕 Gerenciamento de gatos (V2 - Recomendado)
  - name: V2 - Feedings
    description: 🆕 Registro de alimentações (V2 - Recomendado)
  - name: V2 - Weight
    description: 🆕 Controle de peso (V2 - Recomendado)
  - name: V2 - Goals
    description: 🆕 Metas de peso (V2 - Recomendado)
  - name: V2 - Schedules
    description: 🆕 Agendamento de alimentações (V2 - Recomendado)
  - name: V2 - Households
    description: 🆕 Gerenciamento de domicílios (V2 - Recomendado)
  - name: Cats
    description: ⚠️ Gerenciamento de gatos (V1 - DEPRECATED)
  - name: Feedings
    description: ⚠️ Registro de alimentações (V1 - DEPRECATED)
  - name: Households
    description: ⚠️ Gerenciamento de domicílios (V1 - DEPRECATED)
  - name: Schedules
    description: ⚠️ Agendamento de alimentações (V1 - DEPRECATED)
  - name: Weight
    description: ⚠️ Controle de peso dos gatos (V1 - DEPRECATED)
  - name: Users
    description: Gerenciamento de usuários
  - name: Notifications
    description: Sistema de notificações
  - name: Statistics
    description: Estatísticas e relatórios
  - name: Upload
    description: Upload de arquivos
  - name: Profile
    description: Perfil público de usuários
  - name: ScheduledNotifications
    description: Notificações agendadas

paths:
  # ==================== AUTH ====================
  /auth/callback:
    get:
      tags: [Auth]
      summary: Callback de autenticação OAuth
      description: Endpoint para callback do Supabase Auth
      responses:
        '302':
          description: Redirecionamento após autenticação
        '400':
          description: Erro no processo de autenticação

  /auth/mobile/register:
    post:
      tags: [Auth]
      summary: Registro de usuário mobile
      description: Cria uma nova conta de usuário via mobile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, name]
              properties:
                email:
                  type: string
                  format: email
                password:
                  type: string
                  minLength: 6
                name:
                  type: string
      responses:
        '201':
          description: Usuário criado com sucesso
        '400':
          description: Dados inválidos

  # ==================== V2 - CATS (RECOMENDADO) ====================
  /v2/cats:
    get:
      tags: [V2 - Cats]
      summary: 🆕 Listar gatos (V2)
      description: Retorna lista de gatos do household do usuário autenticado
      security:
        - bearerAuth: []
      parameters:
        - name: householdId
          in: query
          schema:
            type: string
            format: uuid
          description: Filtrar por household específico (opcional)
      responses:
        '200':
          description: Lista de gatos
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
              examples:
                success:
                  value:
                    success: true
                    data:
                      - id: "uuid"
                        name: "Miau"
                        photo_url: "https://..."
                        birth_date: "2020-01-15"
                        weight: "4.5"
                        household_id: "uuid"
                        owner_id: "uuid"
                        created_at: "2025-01-28T10:00:00Z"
                        updated_at: "2025-01-28T10:00:00Z"
                    count: 1
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
    
    post:
      tags: [V2 - Cats]
      summary: 🆕 Criar gato (V2)
      description: Adiciona um novo gato ao household
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, householdId]
              properties:
                name:
                  type: string
                householdId:
                  type: string
                  format: uuid
                photoUrl:
                  type: string
                birthdate:
                  type: string
                  format: date-time
                weight:
                  type: number
                feeding_interval:
                  type: integer
                  minimum: 1
                  maximum: 24
      responses:
        '201':
          description: Gato criado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'

  /v2/cats/{catId}:
    get:
      tags: [V2 - Cats]
      summary: 🆕 Buscar gato por ID (V2)
      description: Retorna detalhes de um gato específico
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dados do gato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: ID inválido
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Gato não encontrado
    
    put:
      tags: [V2 - Cats]
      summary: 🆕 Atualizar gato (V2)
      description: Atualiza informações de um gato
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                  minLength: 1
                photoUrl:
                  type: string
                birthDate:
                  type: string
                  format: date-time
                weight:
                  oneOf:
                    - type: number
                    - type: "null"
                feeding_interval:
                  type: integer
                  minimum: 1
                  maximum: 24
                portion_size:
                  type: number
                  minimum: 0
                restrictions:
                  type: string
                notes:
                  type: string
      responses:
        '200':
          description: Gato atualizado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Dados inválidos
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Gato não encontrado
    
    delete:
      tags: [V2 - Cats]
      summary: 🆕 Deletar gato (V2)
      description: Deleta um gato e todos os registros relacionados
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Gato deletado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: ID inválido
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Gato não encontrado

  /v2/cats/{catId}/next-feeding:
    get:
      tags: [V2 - Cats]
      summary: 🆕 Próxima alimentação do gato (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Informações da próxima alimentação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
              examples:
                success:
                  value:
                    success: true
                    data:
                      nextFeeding: "2025-01-28T15:00:00Z"
                      catId: "uuid"
                      hasSchedules: true
                      lastFeedingTime: "2025-01-28T09:00:00Z"

  # ==================== V2 - FEEDINGS (RECOMENDADO) ====================
  /v2/feedings:
    get:
      tags: [V2 - Feedings]
      summary: 🆕 Listar alimentações (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: householdId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Lista de alimentações
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
    
    post:
      tags: [V2 - Feedings]
      summary: 🆕 Registrar alimentação (V2)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [catId]
              properties:
                catId:
                  type: string
                  format: uuid
                amount:
                  type: number
                notes:
                  type: string
                meal_type:
                  type: string
                  enum: [manual, scheduled]
      responses:
        '201':
          description: Alimentação registrada
        '409':
          description: Alimentação duplicada (muito recente)

  /v2/feedings/{id}:
    get:
      tags: [V2 - Feedings]
      summary: 🆕 Buscar alimentação (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dados da alimentação
    
    put:
      tags: [V2 - Feedings]
      summary: 🆕 Atualizar alimentação (V2)
      description: Atualiza um registro de alimentação
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                amount:
                  type: number
                  minimum: 0
                  exclusiveMinimum: true
                notes:
                  type: string
                  maxLength: 255
                meal_type:
                  type: string
                  enum: [manual, scheduled, automatic]
                unit:
                  type: string
                  enum: [g, ml, cups, oz]
      responses:
        '200':
          description: Alimentação atualizada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Dados inválidos
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Alimentação não encontrada
    
    delete:
      tags: [V2 - Feedings]
      summary: 🆕 Deletar alimentação (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Alimentação deletada

  /v2/feedings/stats:
    get:
      tags: [V2 - Feedings]
      summary: 🆕 Estatísticas de alimentação (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: query
          schema:
            type: string
            format: uuid
          description: Filtrar por gato específico (opcional)
        - name: days
          in: query
          schema:
            type: integer
            default: 7
            minimum: 1
            maximum: 90
      responses:
        '200':
          description: Estatísticas de alimentação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'

  # ==================== V2 - WEIGHT LOGS (RECOMENDADO) ====================
  /v2/weight-logs:
    get:
      tags: [V2 - Weight]
      summary: 🆕 Listar registros de peso (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: catId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Lista de registros de peso
    
    post:
      tags: [V2 - Weight]
      summary: 🆕 Criar registro de peso (V2)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [catId, weight, date]
              properties:
                catId:
                  type: string
                  format: uuid
                weight:
                  type: number
                date:
                  type: string
                  pattern: '^[0-9]{4}-[0-9]{2}-[0-9]{2}$'
                notes:
                  type: string
      responses:
        '201':
          description: Registro criado
    
    put:
      tags: [V2 - Weight]
      summary: 🆕 Atualizar registro de peso (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [catId, weight, date]
              properties:
                catId:
                  type: string
                weight:
                  type: number
                date:
                  type: string
      responses:
        '200':
          description: Registro atualizado
    
    delete:
      tags: [V2 - Weight]
      summary: 🆕 Deletar registro de peso (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Registro deletado

  # ==================== V2 - GOALS (RECOMENDADO) ====================
  /v2/goals:
    get:
      tags: [V2 - Goals]
      summary: 🆕 Listar metas de peso (V2)
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Lista de metas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
    
    post:
      tags: [V2 - Goals]
      summary: 🆕 Criar meta de peso (V2)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [cat_id, goal_name, target_weight]
              properties:
                cat_id:
                  type: string
                  format: uuid
                goal_name:
                  type: string
                target_weight:
                  type: number
                unit:
                  type: string
                  enum: [kg, lbs]
      responses:
        '201':
          description: Meta criada

  # ==================== V2 - SCHEDULES (RECOMENDADO) ====================
  /v2/schedules:
    get:
      tags: [V2 - Schedules]
      summary: 🆕 Listar agendamentos (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: householdId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Lista de agendamentos
    
    post:
      tags: [V2 - Schedules]
      summary: 🆕 Criar agendamento (V2)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [catId, type]
              properties:
                catId:
                  type: string
                  format: uuid
                type:
                  type: string
                  enum: [interval, fixedTime]
                interval:
                  type: integer
                times:
                  type: array
                  items:
                    type: string
      responses:
        '201':
          description: Agendamento criado

  /v2/schedules/{id}:
    get:
      tags: [V2 - Schedules]
      summary: 🆕 Buscar agendamento (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Dados do agendamento
    
    patch:
      tags: [V2 - Schedules]
      summary: 🆕 Atualizar agendamento (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Agendamento atualizado
    
    delete:
      tags: [V2 - Schedules]
      summary: 🆕 Deletar agendamento (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Agendamento deletado

  # ==================== V2 - HOUSEHOLDS (RECOMENDADO) ====================
  /v2/households/{id}/cats:
    get:
      tags: [V2 - Households]
      summary: 🆕 Listar gatos do household (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Lista de gatos
    
    post:
      tags: [V2 - Households]
      summary: 🆕 Adicionar gato ao household (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Gato adicionado

  /v2/households/{id}/invite:
    post:
      tags: [V2 - Households]
      summary: 🆕 Convidar membro (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email:
                  type: string
                  format: email
      responses:
        '200':
          description: Convite enviado ou usuário adicionado

  /v2/households/{id}/invite-code:
    patch:
      tags: [V2 - Households]
      summary: 🆕 Regenerar código de convite (V2)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        '200':
          description: Código regenerado
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'

  /v2/households/join:
    post:
      tags: [V2 - Households]
      summary: 🆕 Entrar em domicílio via código (V2)
      description: Entra em um domicílio usando um código de convite
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - inviteCode
              properties:
                inviteCode:
                  type: string
                  minLength: 1
                  example: "ABC123"
      responses:
        '201':
          description: Usuário entrou no domicílio com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Código inválido ou usuário já pertence a domicílio
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Código de convite inválido

  # ==================== CATS (V1 - DEPRECATED) ====================
  /cats:
    get:
      tags: [Cats]
      summary: ⚠️ Listar gatos (V1 - DEPRECATED)
      description: |
        **DEPRECATED**: Use `/v2/cats` em vez disso.
        Esta rota será removida em 2025-07-28.
        
        Retorna lista de gatos do usuário
      deprecated: true
      parameters:
        - name: householdId
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Lista de gatos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Cat'
    
    post:
      tags: [Cats]
      summary: Criar gato
      description: Adiciona um novo gato
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CatInput'
      responses:
        '201':
          description: Gato criado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cat'

  /cats/{catId}:
    get:
      tags: [Cats]
      summary: Obter gato por ID
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalhes do gato
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Cat'
    
    put:
      tags: [Cats]
      summary: Atualizar gato
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CatInput'
      responses:
        '200':
          description: Gato atualizado
    
    delete:
      tags: [Cats]
      summary: Deletar gato
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Gato deletado com sucesso

  /cats/{catId}/next-feeding:
    get:
      tags: [Cats]
      summary: Próxima alimentação do gato
      description: Retorna informações sobre a próxima alimentação agendada
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Informações da próxima alimentação
          content:
            application/json:
              schema:
                type: object
                properties:
                  nextFeeding:
                    type: string
                    format: date-time
                  schedule:
                    $ref: '#/components/schemas/Schedule'

  # ==================== FEEDINGS (V1 - DEPRECATED) ====================
  /feedings:
    get:
      tags: [Feedings]
      summary: ⚠️ Listar alimentações (V1 - DEPRECATED)
      description: |
        **DEPRECATED**: Use `/v2/feedings` em vez disso.
        Será removido em 2025-07-28.
        
        Retorna histórico de alimentações
      deprecated: true
      parameters:
        - name: catId
          in: query
          schema:
            type: string
        - name: startDate
          in: query
          schema:
            type: string
            format: date
        - name: endDate
          in: query
          schema:
            type: string
            format: date
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
      responses:
        '200':
          description: Lista de alimentações
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Feeding'
    
    post:
      tags: [Feedings]
      summary: Registrar alimentação
      description: Cria um novo registro de alimentação
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeedingInput'
      responses:
        '201':
          description: Alimentação registrada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Feeding'

  /feedings/{id}:
    get:
      tags: [Feedings]
      summary: Obter alimentação por ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalhes da alimentação
    
    put:
      tags: [Feedings]
      summary: Atualizar alimentação
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FeedingInput'
      responses:
        '200':
          description: Alimentação atualizada
    
    delete:
      tags: [Feedings]
      summary: Deletar alimentação
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Alimentação deletada

  /feedings/batch:
    post:
      tags: [Feedings]
      summary: Registrar múltiplas alimentações
      description: Cria múltiplos registros de alimentação de uma vez
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                feedings:
                  type: array
                  items:
                    $ref: '#/components/schemas/FeedingInput'
      responses:
        '201':
          description: Alimentações registradas em lote

  /feedings/last/{catId}:
    get:
      tags: [Feedings]
      summary: Última alimentação do gato
      description: Retorna a última alimentação registrada para o gato
      parameters:
        - name: catId
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Última alimentação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Feeding'

  /feedings/stats:
    get:
      tags: [Feedings]
      summary: Estatísticas de alimentação
      description: Retorna estatísticas detalhadas sobre alimentações
      parameters:
        - name: catId
          in: query
          schema:
            type: string
        - name: days
          in: query
          schema:
            type: integer
            minimum: 1
            maximum: 90
            default: 7
      responses:
        '200':
          description: Estatísticas de alimentação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/FeedingStats'

  # ==================== HOUSEHOLDS ====================
  /households:
    get:
      tags: [Households]
      summary: Listar domicílios
      description: Retorna domicílios do usuário
      responses:
        '200':
          description: Lista de domicílios
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Household'
    
    post:
      tags: [Households]
      summary: Criar domicílio
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name:
                  type: string
      responses:
        '201':
          description: Domicílio criado

  /households/{id}:
    get:
      tags: [Households]
      summary: Obter domicílio por ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalhes do domicílio
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Household'
    
    put:
      tags: [Households]
      summary: Atualizar domicílio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
      responses:
        '200':
          description: Domicílio atualizado
    
    delete:
      tags: [Households]
      summary: Deletar domicílio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Domicílio deletado

  /households/{id}/cats:
    get:
      tags: [Households]
      summary: Gatos do domicílio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Lista de gatos do domicílio

  /households/{id}/members:
    get:
      tags: [Households]
      summary: Membros do domicílio
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Lista de membros
    
    post:
      tags: [Households]
      summary: Adicionar membro
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                userId:
                  type: string
                role:
                  type: string
                  enum: [owner, admin, member]
      responses:
        '201':
          description: Membro adicionado

  /households/{id}/invite-code:
    get:
      tags: [Households]
      summary: Obter código de convite
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Código de convite
          content:
            application/json:
              schema:
                type: object
                properties:
                  inviteCode:
                    type: string

  /households/join:
    post:
      tags: [Households]
      summary: Entrar em domicílio via código
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [inviteCode]
              properties:
                inviteCode:
                  type: string
      responses:
        '200':
          description: Usuário entrou no domicílio

  # ==================== SCHEDULES (V1 - DEPRECATED) ====================
  /schedules:
    get:
      tags: [Schedules]
      summary: ⚠️ Listar agendamentos (V1 - DEPRECATED)
      description: |
        **DEPRECATED**: Use `/v2/schedules` em vez disso.
        Será removido em 2025-07-28.
      deprecated: true
      responses:
        '200':
          description: Lista de agendamentos
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Schedule'
    
    post:
      tags: [Schedules]
      summary: Criar agendamento
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleInput'
      responses:
        '201':
          description: Agendamento criado

  /schedules/{id}:
    get:
      tags: [Schedules]
      summary: Obter agendamento por ID
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalhes do agendamento
    
    put:
      tags: [Schedules]
      summary: Atualizar agendamento
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ScheduleInput'
      responses:
        '200':
          description: Agendamento atualizado
    
    delete:
      tags: [Schedules]
      summary: Deletar agendamento
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '204':
          description: Agendamento deletado

  # ==================== WEIGHT (V1 - DEPRECATED) ====================
  /weight-logs:
    get:
      tags: [Weight]
      summary: ⚠️ Histórico de peso (V1 - DEPRECATED)
      description: |
        **DEPRECATED**: Use `/v2/weight-logs` em vez disso.
        Será removido em 2025-07-28.
      deprecated: true
      parameters:
        - name: catId
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Histórico de peso do gato
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/WeightLog'
    
    post:
      tags: [Weight]
      summary: Registrar peso
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/WeightLogInput'
      responses:
        '201':
          description: Peso registrado

  /weight/goals:
    get:
      tags: [Weight]
      summary: Metas de peso
      parameters:
        - name: catId
          in: query
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Metas de peso do gato
    
    post:
      tags: [Weight]
      summary: Criar meta de peso
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [catId, targetWeight]
              properties:
                catId:
                  type: string
                targetWeight:
                  type: number
                targetDate:
                  type: string
                  format: date
      responses:
        '201':
          description: Meta criada

  # ==================== USERS ====================
  /users/{id}:
    get:
      tags: [Users]
      summary: Obter usuário
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Detalhes do usuário
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
    
    put:
      tags: [Users]
      summary: Atualizar usuário
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
      responses:
        '200':
          description: Usuário atualizado

  /users/{id}/preferences:
    get:
      tags: [Users]
      summary: Preferências do usuário
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Preferências do usuário
    
    put:
      tags: [Users]
      summary: Atualizar preferências
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                notifications:
                  type: boolean
                theme:
                  type: string
                language:
                  type: string
      responses:
        '200':
          description: Preferências atualizadas

  # ==================== NOTIFICATIONS ====================
  /scheduled-notifications:
    get:
      tags: [Notifications]
      summary: Listar notificações agendadas
      responses:
        '200':
          description: Lista de notificações agendadas
    
    post:
      tags: [Notifications]
      summary: Criar notificação agendada
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, message, scheduledFor]
              properties:
                title:
                  type: string
                message:
                  type: string
                scheduledFor:
                  type: string
                  format: date-time
      responses:
        '201':
          description: Notificação criada

  /scheduled-notifications/deliver:
    post:
      tags: [Notifications]
      summary: Entregar notificações pendentes
      description: Processa e envia notificações agendadas
      responses:
        '200':
          description: Notificações entregues

  # ==================== V2 - SCHEDULED NOTIFICATIONS (RECOMENDADO) ====================
  /v2/scheduled-notifications:
    get:
      tags: [ScheduledNotifications]
      summary: 🆕 Listar notificações agendadas (V2)
      description: Retorna lista de notificações agendadas do usuário
      security:
        - bearerAuth: []
      parameters:
        - name: delivered
          in: query
          schema:
            type: boolean
          description: Filtrar por entregue ou não (true/false)
        - name: limit
          in: query
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 100
          description: Número máximo de registros
        - name: offset
          in: query
          schema:
            type: integer
            default: 0
            minimum: 0
          description: Offset para paginação
      responses:
        '200':
          description: Lista de notificações agendadas
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
    
    post:
      tags: [ScheduledNotifications]
      summary: 🆕 Criar notificação agendada (V2)
      description: Agenda uma notificação para ser entregue em uma data futura
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - type
                - title
                - message
                - scheduledFor
              properties:
                type:
                  type: string
                  minLength: 1
                title:
                  type: string
                  minLength: 1
                message:
                  type: string
                  minLength: 1
                scheduledFor:
                  type: string
                  format: date-time
                  description: Data/hora em formato ISO-8601 (UTC) para entrega
                catId:
                  type: string
                  format: uuid
      responses:
        '201':
          description: Notificação agendada criada
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Dados inválidos ou data no passado
        '401':
          description: Não autenticado

  /v2/scheduled-notifications/deliver:
    post:
      tags: [ScheduledNotifications]
      summary: 🆕 Entregar notificações pendentes (V2)
      description: Processa e entrega notificações agendadas que estão vencidas
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Notificações entregues
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '500':
          description: Erro interno do servidor

  # ==================== V2 - STATISTICS (RECOMENDADO) ====================
  /v2/statistics:
    get:
      tags: [Statistics]
      summary: 🆕 Obter estatísticas de alimentação (V2)
      description: Retorna estatísticas consolidadas de alimentação
      security:
        - bearerAuth: []
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [7dias, 30dias, 3meses]
            default: 7dias
          description: Período para calcular estatísticas
        - name: catId
          in: query
          schema:
            type: string
            format: uuid
          description: Filtrar por gato específico (opcional)
      responses:
        '200':
          description: Estatísticas de alimentação
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Parâmetros inválidos
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          description: Perfil não encontrado

  # ==================== STATISTICS ====================
  /statistics:
    get:
      tags: [Statistics]
      summary: Estatísticas gerais
      description: Retorna estatísticas consolidadas do sistema
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [day, week, month, year]
            default: week
      responses:
        '200':
          description: Estatísticas gerais
          content:
            application/json:
              schema:
                type: object
                properties:
                  totalFeedings:
                    type: integer
                  activeCats:
                    type: integer
                  averageDaily:
                    type: number

  # ==================== V2 - UPLOAD (RECOMENDADO) ====================
  /v2/upload:
    post:
      tags: [Upload]
      summary: 🆕 Upload de arquivo (V2)
      description: Faz upload de imagem (foto de gato ou usuário)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required:
                - file
              properties:
                file:
                  type: string
                  format: binary
                  description: Arquivo de imagem
                type:
                  type: string
                  enum: [user, cat, thumbnail]
                  default: user
                  description: Tipo de upload
      responses:
        '201':
          description: Upload realizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Arquivo inválido ou ausente
        '401':
          description: Não autenticado

  # ==================== UPLOAD ====================
  /upload:
    post:
      tags: [Upload]
      summary: Upload de arquivo
      description: Faz upload de imagem (foto de gato)
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              properties:
                file:
                  type: string
                  format: binary
      responses:
        '200':
          description: Upload realizado com sucesso
          content:
            application/json:
              schema:
                type: object
                properties:
                  url:
                    type: string
                  fileId:
                    type: string

  # ==================== V2 - PROFILE (RECOMENDADO) ====================
  /v2/profile/{idOrUsername}:
    get:
      tags: [Profile]
      summary: 🆕 Buscar perfil público (V2)
      description: Retorna perfil público de um usuário (por ID ou username)
      security:
        - bearerAuth: []
      parameters:
        - name: idOrUsername
          in: path
          required: true
          schema:
            type: string
          description: ID do usuário (UUID) ou username
      responses:
        '200':
          description: Perfil público do usuário
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: ID ou username inválido
        '404':
          description: Perfil não encontrado
    
    put:
      tags: [Profile]
      summary: 🆕 Atualizar perfil público (V2)
      description: Atualiza o próprio perfil (usuário deve estar autenticado e ser o dono do perfil)
      security:
        - bearerAuth: []
      parameters:
        - name: idOrUsername
          in: path
          required: true
          schema:
            type: string
          description: ID do usuário (UUID) ou username
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                full_name:
                  type: string
                  minLength: 2
                  maxLength: 100
                username:
                  type: string
                  minLength: 3
                  maxLength: 30
                email:
                  type: string
                  format: email
                avatar_url:
                  type: string
                  format: uri
                timezone:
                  type: string
                  maxLength: 50
      responses:
        '200':
          description: Perfil atualizado com sucesso
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/V2Response'
        '400':
          description: Dados inválidos
        '403':
          description: Acesso negado (usuário não pode atualizar outro perfil)
        '404':
          description: Perfil não encontrado

  # ==================== PROFILE ====================
  /profile/{idOrUsername}:
    get:
      tags: [Users]
      summary: Perfil público do usuário
      parameters:
        - name: idOrUsername
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Perfil público do usuário

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: JWT token obtido via POST /api/auth/mobile
  
  responses:
    Unauthorized:
      description: Não autorizado - Token ausente ou inválido
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              error:
                type: string
                example: "Token de autorização não fornecido ou sessão inválida"
    
    Forbidden:
      description: Acesso negado - Usuário não tem permissão
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
                example: false
              error:
                type: string
                example: "Acesso negado ao household"
  
  schemas:
    V2Response:
      type: object
      required:
        - success
      properties:
        success:
          type: boolean
          description: Indica se a requisição foi bem-sucedida
        data:
          type: object
          description: Dados retornados (quando success=true)
        count:
          type: integer
          description: Número de itens retornados (para listas)
        error:
          type: string
          description: Mensagem de erro (quando success=false)
        details:
          type: object
          description: Detalhes adicionais do erro (quando aplicável)
      example:
        success: true
        data:
          id: "uuid"
          name: "Exemplo"
        count: 1
    
    Cat:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        photoUrl:
          type: string
        birthDate:
          type: string
          format: date
        weight:
          type: number
        householdId:
          type: string
        ownerId:
          type: string
        portionSize:
          type: number
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    CatInput:
      type: object
      required: [name, householdId]
      properties:
        name:
          type: string
        photoUrl:
          type: string
        birthDate:
          type: string
          format: date
        weight:
          type: number
        householdId:
          type: string
        portionSize:
          type: number

    Feeding:
      type: object
      properties:
        id:
          type: string
        catId:
          type: string
        userId:
          type: string
        timestamp:
          type: string
          format: date-time
        mealType:
          type: string
          enum: [breakfast, lunch, dinner, snack]
        portionSize:
          type: number
        notes:
          type: string
        createdAt:
          type: string
          format: date-time

    FeedingInput:
      type: object
      required: [catId, timestamp]
      properties:
        catId:
          type: string
        timestamp:
          type: string
          format: date-time
        mealType:
          type: string
        portionSize:
          type: number
        notes:
          type: string

    FeedingStats:
      type: object
      properties:
        period:
          type: object
          properties:
            days:
              type: integer
            startDate:
              type: string
              format: date
            endDate:
              type: string
              format: date
        totals:
          type: object
          properties:
            feedings:
              type: integer
            dailyAverage:
              type: number
        dailyStats:
          type: array
          items:
            type: object
            properties:
              date:
                type: string
                format: date
              feedings:
                type: integer
        catStats:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              name:
                type: string
              totalFeedings:
                type: integer
              dailyAverage:
                type: number

    Household:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
        ownerId:
          type: string
        inviteCode:
          type: string
        createdAt:
          type: string
          format: date-time

    Schedule:
      type: object
      properties:
        id:
          type: string
        catId:
          type: string
        time:
          type: string
          format: time
        days:
          type: array
          items:
            type: string
        mealType:
          type: string
        active:
          type: boolean

    ScheduleInput:
      type: object
      required: [catId, time, days]
      properties:
        catId:
          type: string
        time:
          type: string
        days:
          type: array
          items:
            type: string
        mealType:
          type: string
        active:
          type: boolean

    WeightLog:
      type: object
      properties:
        id:
          type: string
        catId:
          type: string
        weight:
          type: number
        date:
          type: string
          format: date
        notes:
          type: string

    WeightLogInput:
      type: object
      required: [catId, weight]
      properties:
        catId:
          type: string
        weight:
          type: number
        date:
          type: string
          format: date
        notes:
          type: string

    User:
      type: object
      properties:
        id:
          type: string
        email:
          type: string
        name:
          type: string
        photoUrl:
          type: string
        createdAt:
          type: string
          format: date-time

security:
  - bearerAuth: []

