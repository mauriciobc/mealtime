import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Alimentação", "Histórico e registro de alimentação.");

export default function FeedingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
