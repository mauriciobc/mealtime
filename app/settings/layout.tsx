import { pageMetadata } from "@/lib/metadata";

export const metadata = pageMetadata("Configurações", "Ajuste suas preferências e configurações.");

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
